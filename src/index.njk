{% extends "base.njk" %}

{% block body %}
<div class="home">
  <h1><a href="/">poems</a></h1>
  <div class="poem-links">
    {%- for poem in collections.poem | reverse -%}
      <a class="glyph" href="{{ poem.url }}">
        {{ poem.data.glyph }}
      </a>
    {%- endfor -%}
  </div>
</div>
{% endblock %}
